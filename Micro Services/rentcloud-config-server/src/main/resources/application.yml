server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lakshannv/rentcloud-config-store
          search-paths:
            - 'service-config/*service'